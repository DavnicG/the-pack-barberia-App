// constants/colors.ts
// Colores oficiales del diseño Figma de la Barbería.
// Centralizar colores aquí evita repetir valores en cada pantalla.

export const Colors = {
  background: '#0D0D0D',
  surface: '#1A1A1A',       // ← nuevo — fondo de los inputs
  text: '#FFFFFF',
  textMuted: '#888888',
  accent: '#C9A84C',
  accentDark: '#A8893D',
  border: '#2A2A2A',        // ← nuevo — borde sutil de los inputs
  error: '#E53935',         // ← nuevo — color para mensajes de error
};;